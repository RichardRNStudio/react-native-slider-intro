import { Animated } from 'react-native';

export type AnimatedValues = {
  _moveSlideTranslateX: Animated.Value;
  _slideDotScaleX: Animated.Value;
  _slideDotTranslateX: Animated.Value;
  _opacityOfNextButton: Animated.Value;
  _opacityOfDoneButton: Animated.Value;
  _opacityOfSkipButton: Animated.Value;
};
